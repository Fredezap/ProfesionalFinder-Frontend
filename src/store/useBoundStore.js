// import { createMessageSlice } from './slices/createMessageSlice';
// import { createRoutesSlice } from './slices/createRoutesSlice';
// import { createUserSlice } from './slices/createUserSlice';
// import { createWithEqualityFn } from 'zustand/traditional'

// export const useBoundStore = createWithEqualityFn((set, get) => ({
//     ...createMessageSlice(set, get),
//     ...createRoutesSlice(set, get),
//     ...createUserSlice(set, get),
//   }));

// TODO: Check if we can make it work. documentation here:
// TODO: https://docs.pmnd.rs/zustand/guides/slices-pattern